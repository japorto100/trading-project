# DB Migrations

Placeholder for sqlc + pgxpool migration files (Phase 20 / pre-Phase 11 eval).

- Engine: PostgreSQL (sqlc.yaml configured)
- Queries: `internal/db/queries/*.sql`
- Generated: `internal/repository/`

Not active during SQLite phases. Enable when Go DB migration begins.
