GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=9060
GCT_GRPC_ADDRESS=127.0.0.1:9052
GCT_JSONRPC_ADDRESS=127.0.0.1:9053
GCT_USERNAME=replace-me
GCT_PASSWORD=replace-me
# Optional encrypted credentials (AES-GCM base64 payloads, decrypted via *_AES256_KEY_B64)
GCT_USERNAME_ENC=
GCT_PASSWORD_ENC=
# Optional encrypted backtest credentials (falls back to GCT_USERNAME/GCT_PASSWORD if empty)
GCT_BACKTEST_USERNAME_ENC=
GCT_BACKTEST_PASSWORD_ENC=
GCT_HTTP_TIMEOUT_MS=4000
GCT_HTTP_RETRIES=1
GCT_JSONRPC_INSECURE_TLS=false
GCT_PREFER_GRPC=true
ECB_RATES_URL=https://www.ecb.europa.eu/stats/eurofxref/eurofxref-daily.xml
ECB_HTTP_TIMEOUT_MS=4000
FINNHUB_BASE_URL=https://finnhub.io/api/v1
FINNHUB_WS_BASE_URL=wss://ws.finnhub.io
FINNHUB_API_KEY=
FINNHUB_HTTP_TIMEOUT_MS=4000
FRED_BASE_URL=https://api.stlouisfed.org/fred
FRED_API_KEY=
FRED_HTTP_TIMEOUT_MS=4000
ACLED_BASE_URL=https://acleddata.com/api
ACLED_API_TOKEN=
ACLED_EMAIL=
ACLED_ACCESS_KEY=
ACLED_HTTP_TIMEOUT_MS=5000
CRISISWATCH_RSS_URL=https://www.crisisgroup.org/rss-0
CRISISWATCH_HTTP_TIMEOUT_MS=5000
CRISISWATCH_CACHE_TTL_MS=300000
CRISISWATCH_CACHE_PERSIST_PATH=
GEOPOLITICAL_GAMETHEORY_URL=http://127.0.0.1:8091
GEOPOLITICAL_GAMETHEORY_TIMEOUT_MS=5000
NEWS_HTTP_TIMEOUT_MS=4000
NEWS_HTTP_RETRIES=1
NEWS_RSS_FEEDS=https://feeds.marketwatch.com/marketwatch/topstories/
GDELT_BASE_URL=https://api.gdeltproject.org/api/v2/doc/doc
GDELT_HTTP_TIMEOUT_MS=5000
GDELT_HTTP_RETRIES=1
FINVIZ_RSS_BASE_URL=https://finviz.com/rss.ashx
GCT_STRATEGY_EXAMPLES_DIR=vendor-forks/gocryptotrader/backtester/config/strategyexamples

# ---------------------------------------------------------------------------
# ADDITIONS (2026-02-22, Auth/Security + Transitional Proxies - Go Gateway)
# ---------------------------------------------------------------------------
# Shared auth secret for Go JWT validation scaffold (falls back to AUTH_SECRET, then NEXTAUTH_SECRET if empty)
AUTH_JWT_SECRET=
# Auth.js/next-auth shared secret (preferred fallback for mixed Next.js/Go deployments)
AUTH_SECRET=
# Legacy/shared secret key name (secondary fallback used by Go JWT middleware)
NEXTAUTH_SECRET=
# Optional strict JWT claim checks (transitional hardening toward final Auth.js token format)
AUTH_JWT_ISSUER=
AUTH_JWT_AUDIENCE=
# Allowed HMAC JWT signing algorithms (comma-separated; default HS256)
AUTH_JWT_ALLOWED_ALGS=HS256
# Optional clock skew tolerance for JWT time claims (seconds)
AUTH_JWT_LEEWAY_SEC=0

# Security/RBAC/RateLimit scaffolds (flag-gated, default off)
AUTH_JWT_ENFORCE=false
AUTH_RBAC_ENFORCE=false
AUTH_RATE_LIMIT_ENFORCE=false
# Global test/dev bypass for frontend->go auth enforcement (forces JWT/RBAC/RateLimit off)
# Local dev/CI smoke only. MUST stay false / unset in production deployments.
AUTH_STACK_BYPASS=false
# Optional mirror of frontend public flag for single-knob local runs
NEXT_PUBLIC_AUTH_STACK_BYPASS=false
# Emergency-only override for production (keep false/unset by default)
ALLOW_PROD_AUTH_STACK_BYPASS=false
CORS_ALLOWED_ORIGINS=http://127.0.0.1:3000,http://localhost:3000

# JWT revocation blocklist (transitional scaffold)
# Comma-separated JTI preload list for emergency startup revocations
AUTH_JWT_REVOKED_JTIS=
# Default TTL used when a revoked JTI has no explicit exp (ms)
AUTH_JWT_BLOCKLIST_DEFAULT_TTL_MS=900000
# In-memory revocation audit ringbuffer capacity
AUTH_JWT_REVOCATION_AUDIT_CAPACITY=200
# Persistent append-only JSONL revocation audit (hash-chain, tamper-evident)
AUTH_JWT_REVOCATION_AUDIT_JSONL_ENABLED=true
AUTH_JWT_REVOCATION_AUDIT_JSONL_PATH=data/audit/jwt-revocations.jsonl
# Optional SQLite revocation audit persistence (Go-native DB baseline; GCT audit remains JSONL scaffold)
AUTH_JWT_REVOCATION_AUDIT_DB_ENABLED=false
AUTH_JWT_REVOCATION_AUDIT_DB_PATH=data/audit/jwt-revocations.db
# GCT hardening scaffolds (Phase 1c)
GCT_ENFORCE_HARDENING=false
GCT_ALLOW_WEAK_CREDENTIALS=false
GCT_ALLOW_INSECURE_TLS=false
GCT_AUDIT_ENABLED=true
GCT_AUDIT_JSONL_PATH=data/audit/gct-actions.jsonl
# Optional AES-GCM key for future encrypted exchange-key blobs (32-byte key, base64)
GCT_EXCHANGE_KEYS_AES256_KEY_B64=
# Optional AES-GCM key for encrypted GCT service credentials (`GCT_*_ENC`)
GCT_SERVICE_CREDS_AES256_KEY_B64=

# Transitional Python service proxies used by Go gateway
FINANCE_BRIDGE_URL=http://127.0.0.1:8092
# Optional failover replicas (comma-separated). OHLCV path tries sequentially on network/5xx errors.
FINANCE_BRIDGE_URLS=
FINANCE_BRIDGE_HTTP_TIMEOUT_MS=8000
INDICATOR_SERVICE_URL=http://127.0.0.1:8090
INDICATOR_SERVICE_TIMEOUT_MS=8000
GEOPOLITICAL_SOFT_SIGNAL_URL=http://127.0.0.1:8091
GEOPOLITICAL_SOFT_SIGNAL_TIMEOUT_MS=8000
# Adaptive router config scaffold (Phase 0a)
ADAPTIVE_ROUTER_CONFIG_PATH=config/provider-router.yaml
ADAPTIVE_ROUTER_REQUIRED=false

# ---------------------------------------------------------------------------
# ADDITIONS (2026-02-23, Reference G4 Start - BCB/SGS)
# ---------------------------------------------------------------------------
# Banco Central do Brasil (SGS) time-series adapter (no API key required)
BCB_BASE_URL=https://api.bcb.gov.br
BCB_HTTP_TIMEOUT_MS=4000
# Banxico SIE (Mexico) time-series adapter (free token)
BANXICO_BASE_URL=https://www.banxico.org.mx/SieAPIRest
BANXICO_API_TOKEN=
BANXICO_HTTP_TIMEOUT_MS=4000
# Bank of Korea ECOS (Korea) time-series adapter (API key required)
BOK_ECOS_BASE_URL=https://ecos.bok.or.kr
BOK_ECOS_API_KEY=
BOK_ECOS_HTTP_TIMEOUT_MS=4000
# BCRA Principales Variables v4 (Argentina, public JSON)
BCRA_BASE_URL=https://api.bcra.gob.ar
BCRA_HTTP_TIMEOUT_MS=4000
# TCMB EVDS3 (Turkey, public EVDS3 endpoint discovered via UI network trace)
TCMB_EVDS_BASE_URL=https://evds3.tcmb.gov.tr
TCMB_EVDS_HTTP_TIMEOUT_MS=4000
# RBI DBIE gateway (India) FX reserves slice via session-token handshake
RBI_DBIE_BASE_URL=https://data.rbi.org.in
RBI_DBIE_HTTP_TIMEOUT_MS=5000

# Phase 9e GeoMap ingest mode (next-proxy | go-owned-gateway-v1). Must match frontend when using shared flow.
GEOPOLITICAL_INGEST_SHADOW_COMPARE=
GEOPOLITICAL_INGEST_HARD_MODE=next-proxy
GEOPOLITICAL_INGEST_SOFT_MODE=next-proxy
GEOPOLITICAL_ADMIN_SEED_MODE=next-proxy
