# Archived Vendor Forks

Legacy vendor snapshots are no longer used by the active `go-backend` runtime.

Current archive location:

- `archive/go-backend/vendor-forks/gocryptotrader`

Active GoCryptoTrader source for local builds and `go.mod replace`:

- `go-backend/go-crypto-trader`
