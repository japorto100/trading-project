export function MapFooterStatus() {
	return (
		<footer className="border-t border-border px-4 py-2 text-xs text-muted-foreground">
			SSE, candidate workflow, timeline, source health, region news, CFR/CrisisWatch context panel,
			GeoPulse graph insights, drawing primitives, and viewport interaction status are active.
			Shortcuts: Esc cursor, M marker, L line, P polygon, T text, Del delete, Ctrl+Z undo,
			Ctrl+Shift+Z/Ctrl+Y redo, C candidates, R regions, H heatmap, S soft signals.
		</footer>
	);
}
