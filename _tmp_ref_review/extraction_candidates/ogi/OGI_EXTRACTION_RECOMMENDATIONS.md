# OGI Extraction Recommendations

## Kontext

- Quelle: `D:/tradingview-clones/_tmp_ref_review/geo/ogi`
- Mirror-Ziel: `D:/tradingview-clones/_tmp_ref_review/extraction_candidates/ogi_mirror`
- Graph-UI-Ziel: `D:/tradingview-clones/_tmp_ref_review/extraction_candidates/graph-ui`
- Scope: Fullstack-relevante Kandidaten, Supabase/Vendor-spezifische Teile ausgeschlossen.

## Kategorien

- `adopt-as-is`: Direkt nutzbar mit minimalen Anpassungen.
- `adapt-mit-wrapper`: Über Adapter/Wrapper integrieren (empfohlen für Kernlogik).
- `reference-only`: Als Architekturvorlage nutzen, nicht direkt übernehmen.

## Graph UI Startpaket (empfohlene Lesereihenfolge)

1. `frontend/src/components/GraphCanvas.tsx` (im graph-ui Bundle)
2. `frontend/src/stores/graphStore.ts` (im graph-ui Bundle)
3. `frontend/src/lib/graphLayouts.ts` (im graph-ui Bundle)
4. `frontend/src/components/Layout.tsx` (im graph-ui Bundle)
5. `frontend/src/components/Toolbar.tsx` (im graph-ui Bundle)
6. `frontend/src/components/ContextMenu.tsx` (im graph-ui Bundle)
7. `frontend/src/components/SearchBar.tsx` (im graph-ui Bundle)
8. `frontend/src/components/FilterPanel.tsx` (im graph-ui Bundle)
9. `frontend/src/components/TableView.tsx` (im graph-ui Bundle)
10. `frontend/src/components/AnalysisPanel.tsx` (im graph-ui Bundle)
11. `frontend/src/components/MapView.tsx` (im graph-ui Bundle)
12. `frontend/src/stores/sigmaRef.ts` (im graph-ui Bundle)

## Dateimatrix (jede extrahierte Datei genau eine Kategorie)

| Datei | Kategorie | Warum wichtig | Integrationshinweis | Risiko/Caveat |
|---|---|---|---|---|
| `.env.example` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `.github/release.yml` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `.github/workflows/ci.yml` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `.github/workflows/cut-release.yml` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `.github/workflows/docker-publish.yml` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `.github/workflows/prepare-release-pr.yml` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `LEGAL.md` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `backend/entrypoint.sh` | `adopt-as-is` | Sofort nutzbare Vorlage mit geringem Anpassungsbedarf. | Direkt übernehmen und nur Projektnamen/Endpoints/Env-Namen anpassen. | Kleine Drift bei Pfaden/Secrets, daher kurz nachziehen. |
| `backend/tests/test_analysis.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_cli_config.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_cli_installer.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_dependencies.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_graph_engine.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_models.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_registry_popularity.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_sandbox_runner.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_transform_engine.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/tests/test_transforms.py` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `docs/implementation_plan.md` | `adopt-as-is` | Gute Architektur- und Rollout-Vorlage für eure Umsetzung. | Als internes Blueprint-Dokument übernehmen und Schritte auf eure Milestones mappen. | Planannahmen können veraltet sein, daher gegen aktuellen Code prüfen. |
| `docs/implementation_plan_transform_hub.md` | `adopt-as-is` | Gute Architektur- und Rollout-Vorlage für eure Umsetzung. | Als internes Blueprint-Dokument übernehmen und Schritte auf eure Milestones mappen. | Planannahmen können veraltet sein, daher gegen aktuellen Code prüfen. |
| `frontend/src/components/AuthPage.test.tsx` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `frontend/src/components/ProtectedRoute.test.tsx` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `frontend/src/components/ShareDialog.test.tsx` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `frontend/src/lib/entitySearch.test.ts` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `frontend/src/lib/graphLayouts.test.ts` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `frontend/src/stores/graphStore.test.ts` | `adopt-as-is` | Beschleunigt Qualitätssicherung mit direkt verwertbaren Testmustern. | Testfälle an eure APIs/Stores anpassen und als Regression-Basis nutzen. | Mocking ist teils eng an OGI-Interfaces gebunden. |
| `backend/alembic/versions/8e4a7d2c1f21_add_origin_source_to_entities.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/a1b2c3d4e5f6_add_username_entity_type.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/b7c1d2e3f4a5_add_agent_runs_and_steps.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/c92513d84508_initial_schema.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/d4e5f6a7b8c9_merge_agent_and_username_heads.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/e5f6a7b8c9d0_add_agent_user_settings.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/alembic/versions/f2b9c4d7a1e3_add_system_audit_logs.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/context.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/llm_provider.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/models.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/orchestrator.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/run_worker.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/settings_models.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/settings_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/tool_implementations.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/agent/tools.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/agent.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/api_keys.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/dependencies.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/discover.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/edges.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/entities.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/events.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/graph.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/import_.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/locations.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/map.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/plugins.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/projects.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/router.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/api/timeline.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/db/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/db/alembic_runner.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/db/migrations.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/analysis.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/graph_engine.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/transform_engine.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/transform_execution_service.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/api_key.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/edge.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/entity.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/eventing.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/graph.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/plugin.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/project.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/transform.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/transform_settings.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/models/user_plugin_preference.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/api_key_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/audit_log_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/edge_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/entity_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/location_search_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/map_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/nearby_network_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/project_event_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/project_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/sun_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/system_audit_log_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/timeline_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/timezone_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/transform_run_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/transform_settings_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/user_plugin_preference_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/store/weather_store.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/worker/__init__.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/worker/run_worker.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/worker/transform_job.py` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/uv.lock` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/App.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/AnalysisPanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ApiKeySettings.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/AuthPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/BetaNoticeDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ContextMenu.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/CookieConsentBanner.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/DiscoverPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/EntityInspector.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/EntityPalette.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/EventingPanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/FilterPanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/GoogleAnalytics.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/GraphCanvas.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/KeyboardShortcutsDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/LandingPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/Layout.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/MapView.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/MyProjectsPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/PluginManager.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ProfileDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ProjectRealtimeBridge.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ResetPasswordPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/RightToolbar.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/SearchBar.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/Seo.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/ShareDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TableView.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TermsPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TimelinePanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/Toolbar.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TransformPanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TransformResults.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TransformSettingsDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/TransformsCatalogPage.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/AgentSettingsDialog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/InvestigatorControls.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/InvestigatorPanel.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/InvestigatorPrompt.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/InvestigatorStepLog.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/steps/StepApproval.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/steps/StepError.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/steps/StepSummary.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/steps/StepThinking.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/investigator/steps/StepToolCall.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/BrowseTab.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/CategoryFilter.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/InstallButton.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/InstalledTab.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/TransformCard.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/TransformDetail.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/TransformHub.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/UpdatesTab.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/components/marketplace/VerificationBadge.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/hooks/useAnalytics.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/hooks/useIsViewer.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/hooks/useKeyboardShortcuts.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/lib/entitySearch.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/lib/graphLayouts.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/lib/pluginRisk.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/main.tsx` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/betaNoticeStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/cookieConsentStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/graphStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/investigatorStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/projectStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/registryStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/sigmaRef.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/transformJobStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/stores/undoStore.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/agent.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/edge.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/entity.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/eventing.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/graph.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/location.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/map.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/project.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/registry.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/timeline.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/src/types/transform.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `frontend/vite.config.ts` | `adapt-mit-wrapper` | Hoher Reuse-Wert als Kernbaustein eurer Plattform. | Über stabile Adapter (API/Store/Service-Wrapper) integrieren statt direkt koppeln. | Schnittstellen-Drift und implizite Annahmen können sonst Folgekosten erzeugen. |
| `backend/ogi/engine/entity_registry.py` | `reference-only` | Wertvoll als Architektur-Referenz bei hoher Kopplung. | Designprinzipien übernehmen, Implementierung an eure Sicherheits-/Ops-Regeln neu schreiben. | Direktes Übernehmen birgt Integritäts- und Betriebsrisiko. |
| `backend/ogi/engine/plugin_engine.py` | `reference-only` | Wertvoll als Architektur-Referenz bei hoher Kopplung. | Designprinzipien übernehmen, Implementierung an eure Sicherheits-/Ops-Regeln neu schreiben. | Direktes Übernehmen birgt Integritäts- und Betriebsrisiko. |
| `backend/ogi/engine/sandbox_runner.py` | `reference-only` | Wertvoll als Architektur-Referenz bei hoher Kopplung. | Designprinzipien übernehmen, Implementierung an eure Sicherheits-/Ops-Regeln neu schreiben. | Direktes Übernehmen birgt Integritäts- und Betriebsrisiko. |
| `backend/ogi/models/auth.py` | `reference-only` | Enthält projektspezifische Auth- und Rollenlogik. | Nur Konzepte für Guarding/Role-Checks extrahieren. | Direkter Import führt oft zu Vendor- oder Schema-Kopplung. |

## Zusammenfassung

- `adopt-as-is`: 26 Dateien
- `adapt-mit-wrapper`: 157 Dateien
- `reference-only`: 4 Dateien
- Gesamt: 187 Dateien
